name: Publish
on:
  push:
    branches: [ '**' ]
    paths-ignore:
      - '**.md'
      - 'docker/**'
      - '.github/workflows/docker.yml'
  release:
    types: [ published ]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_ACTOR: ${{ github.repository_owner }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build and Publish
        run: ./gradlew build publish --no-daemon -PGITHUB_ACTOR=$GITHUB_ACTOR -PGITHUB_TOKEN=$GITHUB_TOKEN

      - name: Upload main Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PulseLink.jar
          path: main/build/libs/pulselink-*.jar

      - name: Upload plugin Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PulseLink-Plugin.jar
          path: plugin/build/libs/pulselink-plugin-*.jar

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download main Artifact
        uses: actions/download-artifact@v4
        with:
          name: PulseLink.jar

      - name: Download plugin Artifact
        uses: actions/download-artifact@v4
        with:
          name: PulseLink-Plugin.jar

      - name: Upload Artifacts to GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: pulselink-*.jar
          allowUpdates: true
          omitBodyDuringUpdate: true
          omitDraftDuringUpdate: true
          omitNameDuringUpdate: true
          omitPrereleaseDuringUpdate: true
